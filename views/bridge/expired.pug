extends _layout

block bridgeContent
  div(class="w-full py-16")
    div(class="text-center")
      h1(class="text-2xl font-semibold text-[var(--text)] mb-4") Session expired
      p(class="text-[var(--muted)] mb-8") Restart deposit.
      button(type="button", data-expired-restart,
        class="min-h-[44px] px-8 rounded-xl bg-[var(--green)] text-white font-semibold hover:bg-[var(--green-hover)]") Restart
