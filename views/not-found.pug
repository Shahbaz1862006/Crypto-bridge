extends layout

block content
  main(class="min-h-screen flex items-center justify-center px-6", style="background:var(--bg);")
    div(class="text-center max-w-md")
      h1(class="text-5xl font-bold text-[var(--text)] mb-4") 404
      p(class="text-[var(--muted)] mb-8") This page does not exist.
      a(href=ROUTES.MERCHANT.HOME,
        class="inline-flex min-h-[44px] px-8 items-center justify-center rounded-xl bg-[var(--green)] text-white font-semibold hover:bg-[var(--green-hover)]") Back to Merchant
