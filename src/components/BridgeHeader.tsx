/**
 * Simple "Fastpikeswop" label for Bridge flow screens.
 * Renders top-left, above step indicator.
 * margin-top: 24px from top container, margin-bottom: 16px from step indicator.
 */
export function BridgeHeader() {
  return (
    <div className="mb-4">
      <span className="text-[18px] font-semibold text-[#E5E7EB]">
        Fastpikeswop
      </span>
    </div>
  );
}
